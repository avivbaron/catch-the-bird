class Bird {
  // create the birds
  constructor() {
    this.x = -10;
    this.y =  ((cvs.height)/2);
  }

  // reset the bird to the start position
  reset() {
    this.x = -10;
    this.y =  ((cvs.height)/2);
  }
};
